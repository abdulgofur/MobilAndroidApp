export default class Customer {
    constructor(cif, firstName, lastName, birthDate, email, password) {
      this.cif = cif,
      this.firstName = firstName,
      this.lastName = lastName,
      this.birthDate= birthDate,
      this.email = email,
      this.password = password
    }
}
